import React from 'react';
import { BrowserRouter } from 'react-router-dom';

const RootRouter: React.FC = () => {
  return <BrowserRouter>Route들을 import 해서 사용하면 될 듯</BrowserRouter>;
};

export default RootRouter;
